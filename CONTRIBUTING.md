1. Ветки — `main`, `develop`, `feature/*`, `bugfix/*`, `hotfix/*`.
2. Коммиты — типизированные (`feat:`, `fix:` и т.д.).
3. Код — только через PR с ревью.
4. Синхронизация — ребейз или мёрж перед PR.
5. Релизы — теги на `main`.
6. Запрещено — коммитить напрямую в main.
